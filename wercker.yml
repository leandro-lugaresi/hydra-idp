box: toscale/golang-node:latest
build:
  steps:
    - setup-go-workspace
    - npm-install
    - script:
        name: install
        code: |
          make vendor_install
    - script:
        name: build
        code: |
          make build
          ls -la $GOPATH/bin
